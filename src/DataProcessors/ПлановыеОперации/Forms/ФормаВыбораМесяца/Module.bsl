
&НаКлиенте
Процедура Месяц1(Команда)
	
	НомерМесяца = Число(СтрЗаменить(Команда.Имя,"Месяц",""));
	Если Открыта() Тогда
		Закрыть(Дата(Год, НомерМесяца, 1));
	КонецЕсли; 
	
КонецПроцедуры

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Если ЗначениеЗаполнено(Параметры.ДатаПериода) Тогда
		Год = Год(Параметры.ДатаПериода);
	Иначе
		Год = Год(ТекущаяДатаСеанса());
	КонецЕсли; 
КонецПроцедуры
